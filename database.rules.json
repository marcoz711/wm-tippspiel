{
  "rules": {
    ".read": "auth != null",
    "players": {
      "$pid": {
        ".write": "auth != null",
        ".validate": "newData.hasChildren(['name', 'emoji'])"
      }
    },
    "tips": {
      "$pid": {
        "$matchId": {
          ".write": "auth != null",
          ".validate": "newData.hasChildren(['h', 'a']) && newData.child('h').isNumber() && newData.child('a').isNumber()"
        }
      }
    },
    "results": {
      "$matchId": {
        ".write": "auth != null && newData.child('k').val() === 'wmtipp-rk-02fd88bd8e8f'",
        ".validate": "newData.child('h').isNumber() && newData.child('a').isNumber()"
      }
    },
    "bonus": {
      "$pid": { ".write": "auth != null" }
    },
    "koTeams": {
      "$matchId": { ".write": "auth != null" }
    }
  }
}
